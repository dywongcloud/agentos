---
key: mem-1a423b920920b973-2416
ns: default
created: 1784367310961
updated: 1784367310961
---

## holoiroh --rotate-every ticket rotation + PAIRING.md accuracy pass (COMPLETE locally, push blocked no-remote)

Implemented holoiroh-ticket-rotation-flag (reachable half carved out of pairing by gm-continue):
- src/duration.rs: NEW lib-reachable dependency-free parse_rotate_duration (units s/m/h/d, concatenation like 1h30m; rejects empty/garbage/unitless/zero/overflow). No humantime dep. `pub mod duration;` in lib.rs + `mod duration;` in main.rs; de-duplicated main.rs's private copy so the CLI value_parser points at duration::parse_rotate_duration.
- main.rs: Cli::rotate_every: Option<Duration> + print_pairing_block(ticket,context) (shared startup+rotation, byte-identical) + tokio::select! ticker racing ctrlc shutdown, MissedTickBehavior::Skip, first immediate tick skipped via AtomicBool, re-prints QR+ticket+phrase each real tick. TICKET-REPRINT only; full fresh-keypair rotation is TCC-gated + design-open.
- WITNESS: examples/ticket_rotation_probe.rs PASSED. Commits 37de313, 118dd25, ba32924.

PAIRING.md accuracy (row pairing-md-cites-cargo-test-...): real grep proved ZERO #[test] in mac-daemon/src/. Rewrote every 'cargo test'/'47 passing unit tests'/named-test-fn citation to point at real examples/*_probe.rs (allowlist_probe, auth_gate_probe, control_channel_probe); verified those probes pass so claims are truthful.

Re-scoped two stale-blocker rows:
- holoiroh-nat-traversal-anywhere: code+observability DONE (audit_log.rs ConnectionPath::from_connection reads iroh 1.0.2 Connection::paths(); control_channel.rs:1396 logs connection_path:direct|relay; audit_log_probe witnesses). Only live two-network run remains -> blockedBy external.
- holoiroh-sensitive-app-approval-gating: cited already-RESOLVED policy-wrapper row (stale). policy::classify+decide fully consume sensitive_categories (class-5->RequireSensitiveApproval / AlwaysAllow->Allow / HardBlock->Reject), witnessed by policy_probe. But NO live interception consults it: daemon forwards WHOLE prompts to holo serve (no per-tool-call boundary). Added holoiroh-per-action-policy-interception-stream for that gap; both blockedBy external.

TERMINAL: all 18 pending rows blocked (17 external, 1 partial-reachable-done), empty ready_wave. User unblocks: grant macOS Screen Recording+Accessibility TCC + run daemon on real Mac; deploy iOS app to real iPhone; `git remote add origin <url>` (NO remote configured -- git remote -v empty).
