---
key: mem-cfc0024da4dc329b-927
ns: default
created: 1784341368452
updated: 1784341368452
---

## Resolved mutable: holo-inference-api-key-handling

WebFetch of https://raw.githubusercontent.com/hcompai/holo-desktop-cli/main/.env.example confirms the real env var name is HAI_API_KEY (not the assumed HOLO_API_KEY), with the comment '# H Company Models API key. Get one at https://portal.hcompany.ai. `holo login` provisions this automatically and writes it to ~/.holo/.env. For dev overrides, set it here, in ~/.holo/.env, or as a process env var.' Resolution: the user-supplied key will be written ONLY to a new gitignored file holoiroh/mac-daemon/.env as HAI_API_KEY=<value> once holoiroh/mac-daemon/ exists (currently being scaffolded by the in-flight Workflow); holoiroh/mac-daemon/.gitignore will include .env; the raw key value is never written to .gm/prd.yml, any git-tracked file, commit message, or memorize-fire record -- only the env var name HAI_API_KEY appears in committed code/docs from this point forward.
