---
key: mem-1124e46355179182-2421
ns: default
created: 1784356152465
updated: 1784356152465
---

Implemented holoiroh/mac-daemon/src/executor.rs: backend-agnostic ComputerUseExecutor trait (initialize/execute/observe/pause/resume/cancel/get_capabilities/shutdown) + serde boundary types (ExecutionRun/ExecutorEvent/ExecutorCapabilities/ExecutorConfig/ExecutionTask/RunId/RunOutcome/PauseOutcome/ResumeOutcome) + HoloDesktopExecutor adapting the existing HoloBridge/HoloControlBridge. Seam: all HoloDesktop-specific nouns (ControlMessage/A2A/holo serve/request_id/queue/stop) stay inside HoloDesktopExecutor via a private ControlHandle enum; execute reuses handle_message's existing single-active-task queue, cancel/pause issue ControlMessage::Stop, observe taps the single ControlEvent flow via a tokio broadcast fan-out demuxed by run_id and cuts each stream on its terminal event. pause/resume mapped honestly (no resumable-suspend in HoloDesktop: pause->CanceledNotSuspended, resume->Unsupported, can_pause_resume=false). Also threaded the A2A agent-card protocolVersion through HoloBridge (new pub fn protocol_version()) into ExecutorCapabilities. executor.rs is lib-only (NOT declared in main.rs -- would be dead-code warnings; wiring the live daemon path through the seam is out of scope). Native async-fn-in-trait on edition 2024 with #[allow(async_fn_in_trait)]; observe returns Pin<Box<dyn Stream>> via tokio-stream BroadcastStream (added tokio-stream + thiserror as direct deps, both already in Cargo.lock transitively). Verified via examples/executor_probe.rs (cargo run, no test file): every trait method exercised on the real HoloDesktopExecutor over unreachable A2A (192.0.2.1:1), exit=0 3/3 deterministic, witnessing translation, run-id demux (run-A vs run-B no leak), queue-vs-immediate, the full queued-run lifecycle Accepted->Queued(ahead:0)->Failed(terminal), honest pause/resume/cancel, terminal-stream termination, and observe(unknown) not hanging. VERIFY adversarial sweep caught a real probe defect (10s Part-3 timeout < ~20s = 2x connect_timeout for a queued run's delayed-but-correct terminal) and fixed it to 30s. lib/bin/examples/workspace all build warning-clean. Committed locally as 64c32f1 on branch main. PUSH BLOCKED: git remote -v empty repo-wide (confirmed direct + via git_push verb: 'origin does not appear to be a git repository') -- same pre-existing external blocker as mem-918f7f965fd8cc8c-723; requires user to configure a remote before any push can land. blockedBy: external.
