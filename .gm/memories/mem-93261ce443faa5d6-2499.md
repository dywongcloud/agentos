---
key: mem-93261ce443faa5d6-2499
ns: default
created: 1784351285949
updated: 1784351285949
---

## Resolved: input_request/input_response P0-14 (holoiroh control channel) -- committed locally, push blocked by pre-existing no-remote condition

Implemented Project Aro PRD P0-14 in holoiroh/mac-daemon/src/control_channel.rs: ServerMessage::InputRequest{request_id,kind,context,response_options,expires_at} (kind: InputRequestKind enum credential|mfa|ambiguous_choice|missing_info|sensitive_access_consent) and ClientMessage::InputResponse{request_id,selected_option} (structured choice only). InputRequest doc comment + constructor signature enforce no credential-carrying path; witnessed via a distinct sentinel secret string never appearing in serialized JSON. Expiry-to-safe-pause implemented via PendingInputRequest + wait_for_expiry(&Option<PendingInputRequest>) raced in ControlChannel::accept's tokio::select! against real tokio::time; on expiry emits ControlEvent::DaemonStatus -> ServerMessage::Status (never Error) via input_request_expired_text(). New examples/input_request_probe.rs (no #[cfg(test)], matches repo's no-unit-tests convention) witnessed real serde round-trips for all 5 kinds + InputResponse + real timed expiry (250ms real TTL resolved at 251-252ms measured across two separate runs) + already-past-deadline immediate resolution (~1ms) + None-pending never-resolves-within-300ms. cargo build -p holoiroh-daemon --all-targets: 0 errors, 0 warnings, confirmed on a fresh rebuild. PROTOCOL.md fully updated with wire tables, JSON examples byte-matching real serialized output, the credential-safety contract, the separate future manual_input channel note, and expiry-to-safe-pause docs; also fixed a pre-existing stale control.rs->control_channel.rs filename reference while there. Committed locally at 34ad9dc (main work) and 0dc928f (memorize-fire record) on branch worktree-wf_d8bef722-dba-6. git_push {branch:main} fails with 'fatal: origin does not appear to be a git repository' -- git remote -v confirmed empty directly this session, matching the SAME pre-existing repo-wide no-remote condition independently recorded by multiple prior holoiroh sessions (mem-1930451e2fc473c1-1017, mem-918f7f965fd8cc8c-723, mem-b4738095ff61429c-845-adjacent). This is a genuine external blocker requiring the user to run e.g. `gh repo create` + `git remote add origin <url>` -- not something any session-local action can resolve. A future session with a remote configured should git_push {branch:main} from this worktree (or cherry-pick 34ad9dc/0dc928f) to land the work upstream.
