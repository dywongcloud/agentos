---
key: mem-5ad827b260d3b73c-4828
ns: default
created: 1784365365054
updated: 1784365365054
---

## holoiroh ios-bridge real iroh-live subscribe FFI (PRD holoiroh-ios-iroh-ffi-integration) -- COMPLETE locally, push external-blocked (no remote)

Replaced all extern "C" unimplemented!() stubs in holoiroh/ios-bridge/src/lib.rs with a REAL iroh-live subscribe implementation, verified against vendored iroh-live 5f95758 (~/.cargo/git/checkouts/iroh-live-631d06084fd6c270/5f95758). Verified call chain (do NOT re-guess these): Endpoint::builder(iroh::endpoint::presets::N0).bind().await -> Live::builder(ep).with_router().spawn() (iroh-live/examples/subscribe_test.rs + frame_dump.rs pattern); LiveTicket::from_str(s) has pub endpoint:EndpointAddr + broadcast_name:String (iroh-live/src/ticket.rs); live.subscribe(ticket.endpoint, &broadcast_name).await -> Subscription (live.rs:229); sub.broadcast().video_ready().await -> moq_media::subscribe::VideoTrack (subscribe.rs:688); track.try_recv() non-blocking -> Option<moq_media::format::VideoFrame> (subscribe.rs:1089); frame.rgba_image().as_raw() -> &Vec<u8> RGBA8 w*h*4 normalizing any FrameData (rusty-codecs/src/format.rs:748). iroh_live::media = moq_media re-export; iroh_live::media::{subscribe::VideoTrack, format::VideoFrame} both public -- NO extra Cargo deps needed beyond iroh-live+iroh=1.0.2+tokio. image::RgbaImage::as_raw returns std &Vec<u8> so no image dep.

FFI design: BridgeInner{runtime:Runtime, live:Live, subscription:Mutex<Option<Subscription>>} + SubscriptionInner{track:Mutex<VideoTrack>} behind opaque #[repr(C)] zero-sized-marker pointers. Tokio runtime owned inside crate, block_on for connect/subscribe (async never crosses C ABI). Every fn catch_unwind-guarded (no unwind into C = UB). Errors: negative HoloirohStatus + heap CString error string freed via holoiroh_ios_bridge_free_error_string. HoloirohFrame gained pixel_format:u32 (HOLOIROH_PIXFMT_RGBA8=0). poll_next_frame is caller-alloc-and-fill (BUFFER_TOO_SMALL returns real dims for retry; None+is_closed->ENDED; None open->0). control_send/poll_control_event honestly return HOLOIROH_ERR_UNSUPPORTED (separate holoiroh/control/1 ALPN, not the media path) -- never panic.

BIG FINDING (supersedes prior 'cross-compile not available'): this env HAS Xcode (iPhoneOS SDK 26.4) and aarch64-apple-ios is installable via rustup target add. cargo build -p holoiroh-ios-bridge --target aarch64-apple-ios SUCCEEDS exit 0 -- the ENTIRE iroh-live/moq-media/rusty-codecs/openh264/objc2-av-foundation graph cross-compiles to a physical-device iOS target, staticlib libholoiroh_ios_bridge.a (~1GB debug) with all 9 _holoiroh_ios_bridge_* extern C symbols (nm). No crate blocked it.

Witnessed real: host build 0 warnings; iOS-target build exit 0 + 9 symbols; examples/ffi_probe.rs (cargo run --example, NO unit tests per repo rule) exit 0 -- new->non-null, malformed ticket->-2 INVALID_TICKET, well-formed-unreachable ticket->-3 CONNECT_FAILED 'No addressing information available' (real live.subscribe dial failed cleanly no panic/hang), not-connected subscribe->null, null-arg tolerance, control->-7, clean teardown; header compiles as valid C (clang -fsyntax-only); swift build --sdk iphonesimulator --triple arm64-apple-ios17.0-simulator -> Build complete! (IrohLiveFrameSource.swift compiles vs real iOS SDK). ffi_probe builds a real unreachable ticket via SecretKey::generate() (NO-ARG in iroh 1.0.2, matches iroh-live util.rs:17).

Files: ios-bridge/src/lib.rs (real impl), ios-bridge/Cargo.toml (comment), ios-bridge/cbindgen.toml (new), ios-bridge/include/{HoloirohIosBridge.h,module.modulemap} (new; cbindgen 0.27 SKIPS edition-2024 #[unsafe(no_mangle)] fns so the 9 prototypes are hand-kept 1:1, verified vs nm), ios-bridge/examples/ffi_probe.rs (new), ios/Sources/HoloIrohApp/Video/IrohLiveFrameSource.swift (new; #if canImport(HoloirohIosBridge) real impl + #else compile-honest not-linked stub so package builds unlinked; serial DispatchQueue, query-then-fill scratch, pooled kCVPixelFormatType_32RGBA CVPixelBuffer, .pixelBuffer(pb,pts:.invalid) via onFrame), ios/IROH_FFI.md (fallback->as-built), README.md (honest status). Committed locally 8ef3648, worktree clean.

Honest real-vs-needs-device: REAL+witnessed = C ABI, error/null/panic handling, Rust subscribe wiring compiling+linking (host + aarch64-apple-ios staticlib w/ exported symbols), ffi_probe construction/error/teardown, C header, IrohLiveFrameSource compiling vs iOS SDK. NEEDS device+network+Xcode-link = an actual frame on screen (live daemon over real relay/NAT + Xcode app target linking the .xcframework). NOT 'live video works'.

Push BLOCKED same as every prior holoiroh session: git remote -v empty repo-wide, no remote anywhere (mem-1930451e/mem-372d4ffd). Task explicitly said 'do NOT push -- no remote, expected'. .ci-validated NOT fabricated. Terminal honest state = committed-locally + external-blocked-on-push.
