---
key: mem-ebb2a88f900f3c47-1419
ns: default
created: 1784348931248
updated: 1784348931248
---

## Resolved mutable: mut-1784348904485

grep -n -i queued holoiroh/PROTOCOL.md returns zero matches -- PROTOCOL.md never specifies the 'queued, N ahead' text itself, only the general {type,text} wire shape. The 'required verbatim' claim in control_channel.rs:201 and the matching doc line in holo_bridge/control.rs's Queued variant ('the task asks for') both trace to a PRIOR task's literal requirement, still load-bearing because examples/control_channel_probe.rs:113-122 asserts the exact byte string via assert_eq!(json, r#"{"type":"status","text":"queued, 2 ahead"}"#). Decision: resolve as option (a) from the mutable description -- leave ControlEvent::Queued's exact wire text untouched (breaking it would silently regress a previously-completed, tested task), and do not force TaskState into this or any other from_control_event arm. This matches the current task's own explicit instruction ('read existing code first to find the right integration point -- do not force it in if there's no natural hook, in which case just leave the module ready'). task_state.rs stays a ready, self-contained, exported module (pub mod task_state in lib.rs + main.rs) with no live ControlEvent producer wired to it yet -- documented as such in task_state.rs's own module doc under 'Relationship to holo_bridge::control', and will be flagged plainly in the final user-facing report per PRD row task-state-wire-or-document-no-hook.
