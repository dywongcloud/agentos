---
key: mem-77699e51f5ae0289-1303
ns: default
created: 1784365977091
updated: 1784365977091
---

## Resolved mutable: holoiroh-e2e-witness-blocked-by-tcc-permissions

Re-confirmed this turn that macOS provides NO programmatic path to grant Screen Recording/Accessibility TCC permissions: `tccutil` supports only `reset SERVICE [BUNDLE_ID]` (witnessed: its usage string), never a grant subcommand; TCC.db is SIP-protected ('authorization denied' on direct read). The grant requires an interactive click in System Settings > Privacy & Security, which this sandboxed agent session (process tree rooted at the 'claude' process, not an interactive Terminal.app) cannot perform. This is a genuine, outside-session-authority external blocker -- resolved as blockedBy:external, NOT as a completed live witness. The blocker is fully and honestly consolidated into the PRD row holoiroh-user-action-grant-tcc-and-run-daemon, which tells the user exactly what to do (grant the two permissions on a real Mac, run the daemon) to unblock the entire live-run cluster. Every dependent row's CODE is built and its component-level behavior is witnessed via probes; only the final live-daemon runtime proof is blocked, and only by this user-interactive-only permission grant. Marking this mutable witnessed-as-external so it stops blocking phase state, with the block itself faithfully recorded rather than papered over.
