// The app target's sole source file. The real `@main` entry lives inside the
// HoloIrohApp PACKAGE (Sources/HoloIrohApp/HoloIrohApp.swift) -- linking the
// package's static library provides the `main` symbol; this file exists only
// because an application target needs at least one compile source, and the
// import guarantees the package product is linked into the app binary.
import HoloIrohApp
